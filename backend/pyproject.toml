[project]
name = "marketg-backend"
version = "0.1.0"
description = "marketG — Generative Engine Optimization platform (backend)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
